# --- Upload Settings ---
# These are essential for file uploads, especially profile pictures and attachments
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
file.upload-dir=./uploads/

# --- Thymeleaf Settings ---
# Enable caching for production to improve performance
spring.thymeleaf.cache=true

# --- Logging Configuration ---
# Set logging levels to see what's happening internally
logging.level.org.springframework.security=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=INFO

# --- H2 Settings ---
# H2 console is kept enabled
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
# NOTE: In the H2 login screen, use this JDBC URL: jdbc:h2:file:./data/examdb
# The credentials are: sa / password

# --- JPA/Hibernate Settings ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# --- Security Session Settings ---
# Set the session timeout (in minutes)
server.servlet.session.timeout=30m

# --- Application-specific settings ---
spring.application.name=online-exam
server.port=7890

# -------------------------------------------------------------
# DATABASE SELECTION (Switch between H2 and MySQL here)
# -------------------------------------------------------------

# --- OPTION A: PostgreSQL (For Production/Aiven) ---
spring.datasource.url=jdbc:postgresql://ep-polished-resonance-anbat4qx.c-6.us-east-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_tw8N6klyGvWz
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# --- OPTION B: H2 Local (Comment out MySQL above to use this) ---
# spring.datasource.url=jdbc:h2:file:./data/examdb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=password
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Note: JPA settings are defined above